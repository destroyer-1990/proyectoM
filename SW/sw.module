<?php
/**
 * @file
 * Archivo para empezar a programar con Drupal
 */

/**
 * Implementa hook_menu().
 */
function sw_menu() {
  $items['sw/rest'] = array(
    'title' => 'Saludar',
    'page callback' => 'sw_rest',
    'access callback' => TRUE,
  );
  $items['sw/index'] = array(
    'title' => 'index',
    'page callback' => 'sw_index',
    'access callback' => TRUE,
  );
  return $items;
}

function sw_index(){

  echo $_POST['hola'];
  $nid = 1;
  $node = node_load($nid);                                 
  print($node->title);
  echo '<br>';

  $nid = 2;
  $node = node_load($nid);                                 
  print($node->title);
  echo '<br>';
}
/**
 * Callback que implementa la páina: saludar/hola-mundo.
 */
function sw_rest() {
 	$nid = 1;
	$node = node_load($nid);                                 
  print($node->uid);
  echo '<br>';
  $fecha = getdate( $node->created );
  echo ( $fecha["mday"]."-");
  echo ( $fecha["mon"]. "-");
  echo ( $fecha["year"]);
  echo '<br>';
  
  print($node->title);
  echo '<br>';

  print($node->language);
  echo '<br>';

  $body = field_get_items('node',$node, 'body');
  print $body[0]['value'];
  echo '<br>';

  $body = field_get_items('node',$node, 'field_image');
  print $body[0]['filename'];
  echo '<br>';
  
  $body = field_get_items('node',$node, 'field_image');
  print $body[0]['uri'];
  echo '<br>';

  print_r($node);
}
?>